/**
 * Global Layout System
 * Used for standardizing heights and sticky positioning across the application.
 */
export const LAYOUT = {
  HEADER_HEIGHT: 76,
  HEADER_COLLAPSED_HEIGHT: 64,
} as const;
