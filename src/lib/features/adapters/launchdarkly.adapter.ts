import { FeatureAdapter } from '../feature.types';

export class LaunchDarklyAdapter implements FeatureAdapter {
  public isEnabled(): boolean {
    // Stub implementation for future SDK integration
    return false;
  }
}
